parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        composer: ~
        file_size: ~
        commit_message: ~
        jsonlint: ~
        phpunit: ~
        phpversion: ~
        shell: ~
        xmllint: ~
        yamllint: ~
        phpcs:
            standard: ['PSR2']
